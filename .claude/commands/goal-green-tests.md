---
description: Pursue the 'green-tests' goal until its verify command passes (recipe in .aesop/goals/green-tests.md).
---

Work toward: the full test suite passes

Done means `npm test` exits 0 — run it yourself after every increment; never claim
done without it. Plan first and confirm the plan before executing. Respect the hard
stops: 40 iterations, halt after 3 turns without measurable progress, $25 budget.
Track progress in tasks/todo.md. Prefer native `/goal` for unattended runs.
