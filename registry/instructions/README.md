# Instructions (global vs. local)

See [`global-vs-local.md`](global-vs-local.md) for the two scopes (user-level vs. repo-level), the
exact paths and precedence per harness, and why `AGENTS.md` is the portable canonical file this kit
generates the others from.

The canonical instruction file itself is the repo-root [`../../AGENTS.md`](../../AGENTS.md); rendered
per-harness copies live in [`../../adapters/`](../../adapters/).
