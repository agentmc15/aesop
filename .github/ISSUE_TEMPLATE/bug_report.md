---
name: Bug report
about: Something aesop does is wrong
labels: bug
---

## What happened

<!-- The command you ran, what it did, what you expected. `--json` output helps. -->

## Reproduce

```bash
# minimal steps — ideally against a fresh `aesop init --yes` project
```

## Environment

- aesop version (`npm ls @agentmc15/aesop` or commit SHA):
- Node version / OS:
- Harnesses in `aesop.yaml`:

## Manifest (if relevant)

<!-- The aesop.yaml (or the relevant part). Redact anything private — remember,
     emitted files and issues should reference secret NAMES only, never values. -->
