{
  "permissions": {
    "allow": [
      "Bash(npm test)",
      "Bash(npm run build)",
      "Bash(npm run lint)",
      "Bash(npm:*)",
      "Bash(git commit:*)",
      "Bash(git push origin HEAD)"
    ],
    "ask": [
      "Bash(git push origin main)",
      "Bash(npm publish)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "grep -qE 'rm -rf /|rm -rf ~|git push --force origin (main|master)|git reset --hard origin|DROP (TABLE|DATABASE)|--dangerously-skip-permissions' && { echo 'blocked by aesop: block-dangerous-commands' >&2; exit 2; } || exit 0"
          }
        ]
      }
    ]
  }
}
